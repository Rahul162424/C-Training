#include<stdio.h>
#include<string.h>
int main(){
    char str[]="rahul";
    char target='u';
    char newstr[]
    for(int i=0; i<ln; i++){
          if(str[i]!=target){
            newstr[j]=str[i]
            j++;
          }
    }
}